# Reply drafts (may contain real correspondence)
drafts/
# Generated client sites are built on demand
Demo*/
# Netlify preview build temp dirs
.preview_*/
# Local settings may hold secrets — never commit
.claude/settings.local.json
__pycache__/
*.pyc

# Runtime secrets
.env
ops/.env

triage/

# Regenerate with `python3 ops/call_sheet.py --write`. Derived entirely from leads.csv,
# so committing it duplicates the same names and phone numbers into a second file — and
# while this repo is public that means publishing them twice over.
call_sheet.md
