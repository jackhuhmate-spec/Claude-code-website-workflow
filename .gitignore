# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# Environment & secrets
.env
.env.local
ops/.env
.claude/settings.local.json

# Database
*.db
*.sqlite
*.sqlite3

# TypeScript platform (`packages/`). `dist/` is already ignored above.
node_modules/
.next/
*.tsbuildinfo
.pglite/

# Project specific
drafts/
Demo*/
.preview_*/
triage/
src/omniroute/.env

# Regenerate with `python3 ops/call_sheet.py --write`. Derived entirely from leads.csv,
# so committing it duplicates the same names and phone numbers into a second file — and
# while this repo is public that means publishing them twice over.
call_sheet.md

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
